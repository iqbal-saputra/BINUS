#include <stdio.h>
#include <stdlib.h>

struct Node{
    int data;
    struct Node *next, *prev;
}*head, *tail;

struct Node *newNode(int data){
    struct Node *newNode = 
}
int main(){
    
    return 0;
}